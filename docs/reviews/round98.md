# 第 98 轮体验官审查报告 — #176 回归 + 词典页专项 + 官网下载资产复查 + 核心回归

- 基线：main @ `5d4f274`（含 #176），`npm run pack:dir` 退出码 0，打包版实测
- 环境：Windows Server 2022；防火墙三 profile 全程 OFF；测毕清场（见文末，词典注入条目全部还原）
- 口径：【实测】= 打包版运行实证；【源码】= 代码核对；【未验证/推测】= 如实标注

## 结论

| 级别 | 数量 |
|---|---|
| P0 | 0 |
| P1 | 0 |
| P2 | 0 |
| P3 | 1 |

## ① #176 回归【实测】

- en：「If several rules match, the first one in the list wins.」；zh-CN：「多条规则同时命中时，列表中靠前的一条生效。」均在规则区说明段正常换行显示、无溢出（实拍各一次）。第 97 轮观察项闭环。

## ② 词典页专项

### 300 上限边缘【实测】
- 预置 295 词后粘贴 10 行保存 → 计数 300/300 + 黄条「词典最多 300 个热词，5 个词未能加入。」精确到条数；实际入库 new1-new5、new6-new10 被丢（搜索核对），slice 截断与提示一致【源码 Dictionary.tsx:22-23】。

### 删除与清空
- 逐条删除【实测】：chip × 单击即删，计数 300→299，过滤态下删除后列表正确维持。
- **P3-①：「清空」一键清空整本词典，无确认、无 Undo**【实测】——299 词（含自学习积累）单击即失。与既有交互不一致：历史页 Clear all 有两步确认（第 84 轮核对）、学词/历史删除均有 Undo toast，词典清空是当前唯一无任何后悔药的批量破坏操作；导出可自救但用户未必先导出。修法（择一）：两步确认（同 history.clear 模式 ~10 行）或 Undo toast（复用 #158 胶囊 ~15 行）；词典清空低频，建议两步确认足矣。

### 导出 round-trip【实测】
- 导出 speaktype-dictionary-2026-08-16.txt：299 行一行一词、UTF-8、含中文词与英文词无损；全选复制回贴 + 保存 → 299/300 完整还原，round-trip 闭环。

### 搜索边界【实测】
- 大小写不敏感（HOTWORD293 命中 hotword293）；查询首尾空格自动 trim；无命中显示「没有匹配的热词。」；清词典后空态双行文案正常。

### 热词纠错实际听写【实测】
- 英文热词：口述 "please use speak type to write this note" → 落字 `Please use SpeakType to write this note.`（空格/大小写归并替换）。
- 拼音热词：热词「开慧」+ zh1 音频 → 落字 `今天下午3点开慧，预算是5200元`（同音替换生效；近音归并 z/zh、n/l、in/ing 等【源码 hotwords.ts:10-23】，本机无中文 TTS 无法自由造近音句，标注源码级）。副作用观察：常用词加为热词会全局替换（如上例「开会→开慧」），属设计取舍，词典页已有「只属于你的重要词汇」定位文案，不立案。

## ③ 官网下载资产复查【实测】

- 官网 en 与 /zh/ 页版本号均为 0.15.0，与 Release 一致。
- 三资产 HEAD 200：Setup 103,250,440B / portable 91,664,958B / apk 2,494,687B（直链实查；GitHub API 限流不影响 CDN 直链）。

## ④ 核心回归【实测】

- RightCtrl 中文「今天下午3点开会，预算是5200元」（sensevoice+ITN）+ Alt+Q 免按「我们明天去公园散步」准确识别落字（history UTF-8 核对；还原词典后复测，无热词干扰）。

## 清场记录

- 词典注入条目（295 预置 + new1-5 + 开慧/SpeakType）全部随配置备份还原（还原后 hotwords=0，与本轮开始前一致）；导出的 txt 已删；latest-release.json/transcribe-last.json 已删；历史还原。
- SpeakType 进程 0；无 .part；43117 无监听；防火墙三 profile 保持 OFF。

## 下轮候选

1. P3-①（词典清空两步确认 ~10 行）落地回归。
2. 长期未审面：Home 页信息密度/引导卡与新用户再访体验，或失败录音重试专项复查（20 段/7 天/50MB 上限边缘）。
3. 云端成功路径补测（等 key）/ 真手机麦通道（挂账）。
