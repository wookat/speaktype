# 第 104 轮体验官审查报告 — 异常退出/写入韧性专项 + 免提 soak 复测

- 基线：main @ `cefd198`（含 #185），`npm run pack:dir` 退出码 0，打包版实测
- 环境：Windows Server 2022，虚拟声卡；延迟绝对值仅作同机相对对比
- 口径：【实测】/【源码】/【未验证】/【推测】

## 结论

| 级别 | 数量 |
|---|---|
| P0 | 0 |
| P1 | 0 |
| P2 | 0 |
| P3 | 1 |

## 顺手抽查：#185 官网 EN 修正已上线【实测】

线上 EN 页已无 “Settings → Recognition”，现为 “Settings → Speech”（另有 Settings → General / Apps 表述，均与应用页签一致）。

## ① 异常退出/写入韧性专项

**A. 空闲态强杀→重启完整恢复【实测】过**：强杀前后 history.json 哈希一致；重启后配置/历史/词典完整，首句听写即准确（含 ITN）。

**B. 录音进行中强杀【实测】过**：按住 RightCtrl 播音中途 `Stop-Process -Force`——桌面无残留胶囊/悬浮条（实拍确认），无幽灵进程；重启后无卡死、无坏状态，听写立即正常。

**C. speaktype.json 只读（磁盘写入拒绝降级）——P3-① 立案**：
- 好的一面：应用不崩溃、听写/历史写入（独立文件）完全正常、UI 可操作。
- 问题：设置改动 UI 上即时显示成功（Hold threshold 120→200 实拍），**磁盘未写入、无任何错误提示，重启后静默丢失**；且启动约 2 秒后窗口位置持久化触发 **`uncaughtException EPERM: operation not permitted, rename speaktype.json.tmp-… -> speaktype.json`**（main.log 实录，conf/atomically 原子写重命名被拒），未被捕获仅靠全局兜底、用户无感知。
- 建议（~10 行）：settings 写失败 catch 后 toast「设置无法保存（文件只读/权限不足）」；bounds 持久化包 try/catch 降噪。触发条件为配置文件/目录不可写（备份软件锁定、权限收紧、漫游配置只读），低频但静默丢配置违反「明确错误提示」一贯标准，定 P3。

**D. 磁盘满【未验证】**：本机为单一系统盘，无法低成本安全模拟（填盘有系统风险，quota/小 VHD 无法覆盖固定的 %APPDATA% 路径），按约定标注未验证。EPERM 路径（C 项）可视为写失败家族的近似取样。

## ② Alt+Q 免提 soak 复测（25 分钟）【实测】

方法：每周期 Alt+Q 进免提 → TTS 两句 → Alt+Q 退出，65 周期共 130 句。

| 指标 | 第 45 轮基线 | 本轮 |
|---|---|---|
| 句数丢失 | 0 | **0（130/130 全部进历史+落字）** |
| 内存曲线 | 平稳无泄漏 | 全程 530-625MB 区间波动，无上行趋势 |
| 空闲释放 | 有回落 | 结束后 3 分钟 596→**393MB** |
| 进程存活 | 是 | 是（soak 后核心听写照常） |

抽样准确性正常（"The soak test for around 104…" 等仅个别 TTS 近音差，无结构性错误）。

注：soak 驱动脚本若以隐藏窗口进程启动，TTS 不出声导致全静音段（maxPeak=0）——测试环境摩擦，非产品问题；改为前台会话驱动后正常，已记入经验。

## ③ 核心回归【实测】

RightCtrl 中文「今天下午3点开会，预算是5200元」含 ITN 准确落字（本轮共实测三次：恢复验证、只读态、终验，均过）。

## 清场记录

- speaktype.json 只读属性已解除并核实；配置/历史还原；transcribe-last.json/latest-release.json 删；SpeakType 进程 0；无 .part；43117 无监听；防火墙三 profile 保持 OFF。

## 下轮候选

1. P3-①（设置写失败 toast + bounds try/catch，~10 行）落地回归。
2. 度量脚本第三数据点随下个发版跑。
3. 真手机麦/云端 key 补账（挂账）。
