# 第 96 轮体验官审查报告 — #172 回归 + 统计口径/托盘/开机自启/关窗专项 + FAQ 一致性 + 核心回归

- 基线：main @ `7294f66`（含 #172/#173），`npm run pack:dir` 退出码 0，打包版实测
- 环境：Windows Server 2022；防火墙三 profile 全程 OFF；测毕清场（见文末，含 launchAtLogin 关闭与启动项移除核实）
- 口径：【实测】= 打包版运行实证；【源码】= 代码核对；【未验证/推测】= 如实标注

## 结论

| 级别 | 数量 |
|---|---|
| P0 | 0 |
| P1 | 0 |
| P2 | 0 |
| P3 | 1 |

## ① #172 回归抽样

- **混排标点·英文**【实测】：parakeet+增强标点口述带逗号长句 → 落字 `…it worked great. So we should…`，无「,.」连排（实拍+history 核对），第 95 轮 P3-① 修复生效。
- **混排标点·中文**【源码+node 复算】：CJK 分支新收敛正则对 `开会，。预算…，！` 复算输出 `开会。预算…！` 正确；运行时 whisper/sensevoice 中文句自带标点未触发 punct 模型二次加工，无混排【实测】。
- **升级横幅缓存**：
  - 缓存读路径【实测】：注入 `latest-release.json`（tag=v0.99.0，24h 内）→ 启动后 About 无网络（本机 api.github.com 全局 403）仍显示「New version v0.99.0 is available」横幅——缓存生效、离线可提示。
  - 缓存生成路径【未验证——环境所限】：本机对 api.github.com 匿名请求恒 403（PowerShell 直连同样 403，环境级出口限制非应用问题），删缓存后启动拿不到成功响应故文件未生成；「仅成功才写缓存 + 失败 30min 重试 ≤3 次」逻辑【源码 index.ts:329-351 核对】正确。
- 统计随听写正确累计【实测】：21 词英文句 → words +21、sessions +1、durationMs +实际时长。

## ② 统计口径/托盘/开机自启/关窗专项

### 统计口径【实测+源码】
- Sessions/Words/Time saved 只计听写（含重试成功一次性计入，失败听写不计）；**文件转录不计入统计**：转录 zh1.mp3 前后 stats 逐字段不变（20318/323 → 20318/323），转录条目正常进历史。口径 = 「统计只衡量语音打字节省」，与 Home 文案自洽，符合预期不立案。
- 落字失败（粘贴异常）但识别成功的条目仍计统计【源码 dictation.ts:717】——文本已产出，口径可辩护，观察不立案。

### 托盘【实测】
- 菜单三项：打开 SpeakType / 配置语音识别 / 退出；界面语言切简体中文后托盘菜单**即时**切换（实拍，#refreshTrayMenu on uiLanguage【源码】）；「打开」恢复主窗且状态保留；「退出」真正退出（进程 0 实测）。
- **P3-①：托盘「配置语音识别」直接弹出豆包聊天网页**【实测】——本地离线模型已就绪的用户点它，得到的是一个无上下文说明的字节豆包完整网页（桥接窗口 showBridge【源码 index.ts:253】），既不是设置页也没有任何「这是干嘛的」说明；与产品「本地优先、无上传」的心智强烈冲突，新用户可能误以为必须登录豆包。修法 ~3 行：托盘项改为打开主窗并落 设置→语音识别 页签（showMain+导航），豆包桥接入口只保留在豆包 provider 的设置卡片里。
- 遗留观察：托盘区偶见已退出进程的幽灵图标（Windows 托盘缓存特性，鼠标掠过即消失，非应用缺陷）。

### 开机自启【实测】
- 开启「开机时启动应用」→ HKCU\...\Run 出现 `SpeakType = "<exe>" --hidden`；关闭 → 注册表项即刻移除（前后各核对一次）。
- `--hidden` 恒带但启动时以「开机时不展示应用窗口」设置为准（`startMinimized && --hidden` 双条件【源码 index.ts:464】），语义正确。
- 真实重启后自启行为【未验证】（不重启审查机），注册表面已实证。

### 关窗行为【实测】
- 点 X → 窗口关闭、进程存活（托盘常驻）；托盘态热键听写照常落字（zh2 句实拍）；再从托盘「打开」恢复窗口状态无损。关窗≠退出的行为与同类常驻工具一致，且托盘有明确退出项，不立案。

### 官网 FAQ 一致性抽查【实测+历史轮】
- 「数据只去你配置的服务、密钥/历史/失败录音在 %APPDATA%\SpeakType」——与实测路径一致 ✓
- 「默认离线模型、无需 API key」✓；「Alt+Q 静音自停」✓（历史轮实测）
- 「卸载保留 %APPDATA%（含 660MB 模型），删目录即彻底清除」——第 81 轮卸载实测一致 ✓
- 「快捷键可录制包括鼠标侧键」——录制入口实拍存在；鼠标侧键无硬件【未验证】
- 未发现 FAQ 与实际行为矛盾项。

## ③ 核心回归【实测】

- RightCtrl 中文「今天下午3点开会，预算是5200元」（sensevoice+ITN）+ Alt+Q 免按「我们明天去公园散步」均准确落 Notepad（实拍）。

## 清场记录

- launchAtLogin 已关闭且注册表 Run 项移除核实（run=False）；注入的 latest-release.json 假缓存已删；transcribe-last.json 已删；配置/历史从备份还原。
- SpeakType 进程 0；无 .part；43117 无监听；防火墙三 profile 保持 OFF。

## 下轮候选

1. P3-①（托盘「配置语音识别」改指设置语音页，~3 行）落地回归。
2. 长期未审面继续：多人设规则边界（重叠规则先后/大小写/中文窗口标题）/ 悬浮条字幕行为。
3. 云端成功路径补测（等 key）/ 真手机麦通道（挂账）。
