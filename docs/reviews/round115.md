# 第 115 轮体验官审查报告 — #196 回归 + Transcribe×F8×历史 Correct 组合链路专项

- 基线：main @ `a92b6eb`（含 #196/#197），`npm run pack:dir` 退出码 0，打包版实测
- 口径：【实测】/【源码】/【未验证】/【推测】

## 结论

| 级别 | 数量 |
|---|---|
| P0 | 0 |
| P1 | 0 |
| P2 | 0 |
| P3 | 0 |

零立案，观察 ×3（详下）。

## ① #196 回归【实测】全过（4/4 + 打包产物矩阵 5/5）

打包产物级（asar 提取 shipped `extractCorrections` vm 实跑）：

| 用例 | 结果 |
|---|---|
| 句首单字改（仅 canRight） | `方案→防案` 回扩学入 ✓ |
| 标点后单字改（仅 canRight） | `方案→防案` 回扩学入 ✓ |
| 句中两侧 CJK（歧义） | 只回单字 `园→圆` → 不满足 2 字下限不学 ✓ |
| 句尾单字改（仅 canLeft） | `方案→方按` 回扩学入 ✓ |
| 两侧 CJK 单字改 | 只回单字不学（#196 取舍） |

真实打包 E2E（听写→记事本手改→观察窗结算）：

1. 句首 `今→金` → log `auto-learn: "今天"->"金天"`，词典 +「金天」✓
2. 标点「，」旁 `预→域` → 学入「域算」✓
3. 句中两侧 CJK `开→凯` → 等待 10s 无学入、词典不变 ✓（第 114 轮「圆散」类跨词条目从根上不再产生）
4. 词典纠错落字：词典含「金天」再听写同句 → 落字「金天下午3点开会…」同音纠错生效 ✓

观察①（取舍记录，不立案）：两侧均为 CJK 的单字同音改（如 113 轮「开会→开慧」句中场景）现在也不学——#196「宁可少学」的明知取舍，如实记录供后续分词方案参考。

## ② 专项 b：Transcribe 结果 × F8 改写 × 历史 Correct 组合【实测】

- **转录→历史 Correct**：转录条目（personaName=源文件名）可 Correct，编辑后 `text` 持久化、`raw` 保留原转写、重进页面/搜索「Davide」可检索到 ✓；连续两次 Correct（含全文重写）均正常。
- **Correct→学词建议闭环**：中文条目把「域算」改「贝塔」→ 弹「Add "贝塔" to the dictionary」chip → 点击后词典 +1 ✓（suggestHotword 仅建议 2-6 字纯中文段，英文改动不弹，源码一致）。
- **历史 Correct 不回写转录页/导出（观察②）**：历史改「David→Davide」后，Transcribe 结果面板与再次导出 TXT 均保持「David」（前后导出逐字节一致）。论证：转录页=原始结果、历史=可编辑记录，口径自洽可接受；但若用户期望「纠错后导出」会有落差，候选方向：导出时提示存在历史纠正版或直接采用。不立案。
- **F8 口述指令改写转录文本**：导出 TXT 粘贴记事本，选中一句 + 按住 F8 口述「make this sentence more formal」→ mock 收到 `/v1/chat/completions`（口述指令 + 选中原文完整嵌入 prompt，501B 实录），选区被替换为 mock 返回 ✓。
- **F8 于应用内转录面板（观察③）**：面板分段文本不可选中（拖选/双击/三击均无选区），F8 无法直接作用于面板；「Copy all→外部编辑器→F8」为可用路径。轻量候选：面板文本允许选中。不立案。
- **词典热词同样作用于文件转录【实测】**：词典含「金天/域算」时转录 zh 音频 → 输出「金天下午3点开会，域算是5200元。」——正确的「今天/预算」被同音改写。与听写路径口径一致（设计如此），但把第 114 轮 P3-①（学入条目全局同音替换）的影响面扩大到 Transcribe：修复时（精确串替换方向）应同时覆盖转录路径。

## ③ 核心回归【实测】过

词典清空后：RightCtrl 中文「今天下午3点开会预算是5200元」含 ITN ✓；Alt+Q 免提同句准确落字含标点 ✓。

## 测试摩擦记录（非产品问题）

- 新生成的 Edge TTS 中文语料首词被 sensevoice 误识为英文（「方案」→"Fine"），非产品缺陷；改用旧有已验证 fixture 完成。
- 本机无中文 SAPI 语音（仅 David/Zira），中文 TTS 需走 msedge-tts。

## 清场记录

配置/历史整体还原（词典 0 条、mock polish 配置移除）；mock 停 18099 无监听；测试音频/导出文件删；进程 0；无 .part；43117 无监听；防火墙三 profile 保持 OFF。

## 下轮候选

1. 第 114 轮 P3-①（精确串替换/分词）落地时一并覆盖 Transcribe 路径并回归。
2. 观察②（历史纠正与导出口径）与观察③（面板文本可选中）若采纳一并回归。
3. 真手机麦远程链路 / 云端 ASR·润色真实 key 补账（挂账）。
