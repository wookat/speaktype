# 第 127 轮体验官审查报告 —— #207 回归 + 托盘/关窗生命周期 + 词典页交互边界

- 审查日期：2026-08-17
- 基线：main@8129fe3（含 #207/#208，`npm run pack:dir` 全绿，打包实测）
- 证据分级：【实测】打包运行时；【源码】；【推测】；【未验证】

## 结论总览

**P0=0，P1=0，P2=0，P3=0——零立案，观察 ×2。**

## ① #207 回归（全过）

- **冷启动首次 Alt+Q**：两次冷启动（等 15s 就绪后首个键事件即 Alt+Q）均立即进免按（悬浮条实拍 + 第二次冷启动直接完成免按听写准确落字）——126 轮 P3-① 消除【实测 2/2】。
- **OFF 30 轮兜底**：vadAutoStop=false 免按空聆听，01:11:12 起每 10s 一轮空 finalize，**恰好 30 轮**（log 计数=30，末轮 01:16:16）后自动退出，此后再无新轮——126 轮 P3-②（无限空聆听）消除，约 5 分钟上限与 #207 常量一致【实测】。

## ② 专项 a：托盘与关窗生命周期（全过）

- 主窗口 X 关闭 → 进程常驻（8 子进程），窗口不在时 RightCtrl 听写照常准确落字【实测】。
- 托盘右键菜单三项：Open SpeakType / Set up speech recognition / Quit（实拍）；Open 恢复主窗口且页面状态保留；Quit 干净退出（SpeakType 进程数 0，无残留）【实测】。
- 观察①：菜单项「Set up speech recognition」在模型已配好后仍显示同名文案（源码为固定的设置页快捷入口 tray.activate），已配置用户可能误以为「还没设置好」；候选：配置完成后改为「语音识别设置」类中性文案。
- 观察②：托盘区历史幽灵图标为测试中强杀进程的系统级残留（悬停即消失），与 112 轮裁定一致，非产品问题；正常 Quit 路径图标即时移除【实测】。

## ③ 专项 b：词典页交互边界（全过）

- 混合粘贴（重复词 ×2、前后空格词、>20 字符英文长词、空行、正常词）一次 Save：重复去重、空格修剪、空行跳过、长词拒收，结果 3/300 全部符合占位符说明【实测·实拍】。
- 已存在词再次添加：静默去重不重复计数（4/300）【实测】。
- 搜索「张」精确过滤到「张三」【实测】。
- Clear 两步确认（「Clear all words? Click again」红色确认态）防误清【实测·实拍】。
- 观察（并入观察①不另立）：>20 字符行被静默丢弃无逐行反馈，占位符已写明上限，影响面小。

## ④ 核心回归（过）

- RightCtrl 中文「我明天去公园散步」准确落字；Alt+Q 免按 "The review and the report are done today." 准确落字【实测】。
- 测试基建更正：本轮 zh 用例经 mp3+MediaPlayer 播放路径连续 3 次丢字，换 ffmpeg 转 wav + SoundPlayer 后立即全对——124-126 轮的 zh 偶发丢字确证为 mp3 解码抖动（测试基建），非产品问题；后续轮次 zh 用例改用 wav。

## 测毕清场

- SpeakType/notepad/node 进程 0；43117/18099 无监听；无 .part
- 词典清零（两步 Clear）；config/history 由 round127-*.bak 整体还原（321 条、hold=RightCtrl、toggle=Alt+Q、vadAutoStop=true、模型 parakeet）
- 防火墙三 profiles OFF；repo 回 main、工作区干净
