# 第 122 轮体验官审查报告 —— 专项 b：手机麦 LAN × #200/#201 学词口径 + 专项 c：五语新增文案 ja/ko/zh-TW 运行时实拍

- 审查日期：2026-08-16
- 基线：main@599a419（与 121 轮同提交，`npm run pack:dir` 全绿，SpeakType.exe 构建 2026-08-16 23:11）
- 方法：打包应用运行时实测（ws 模拟手机端 LAN 直连 + keybd_event 热键 + UI 实拍）；不改产品代码
- 证据分级：【实测】打包运行时；【源码】；【推测】；【未验证】

## 结论总览

**P0=0，P1=0，P2=0，P3=0——零立案。**

## ① 专项 b：手机麦 LAN 链路 × #200/#201 新学词逻辑（同口径，全过）

ws 模拟手机端（wss://…:43117/ws?t=token，start → 16k mono PCM → stop），autoLearn 开：

- **远程麦落字后同轮双改学入**【实测】：远程听写「今天下午3点开会，预算是5200元」准确落字（含实时字幕 partial 推送逐段正确），落字后同轮改 会→慧 + 预→域（近距夹标点形态，#200 口径），观察窗结算 auto-learn ×2，词典 +2（开慧/域算）——与本地麦 120 轮同用例同结果。
- **整句重写拒学**【实测】：第二次远程听写落字后全选替换为完全无关英文句，无 auto-learn、词典不变（仍 2 条）——#201 求和口径在远程路径同样生效。
- **学词→纠错闭环（远程路径）**【实测】：第二次远程听写的实时字幕与落字均已被新学词纠正（partial 显示「…开慧，域算…」），学入词条立即作用于远程麦链路。
- 错 token 拒连、busy 抢占等 116 轮已验行为未重测；真手机/中转模式仍【未验证】挂账。

## ② 专项 c：五语 UI 近 20 轮新增文案（全过）

- **键完整性**【源码】：en/ja/ko/zh-CN/zh-TW 五语各 357 键，脚本比对零缺键零多键。
- **ja 运行时实拍**【实测】：空结果 toast「聞き取れませんでした 音声が検出されませんでした — もう一度お試しください」（118 轮统一提示）；历史页全本地化（履歴/コピー 修正 削除/認識原文を表示）；历史 Correct 英文纠正 chip「「binder」を辞書に追加して自動修正しますか？」（#200 新增英文建议路径）渲染正确无溢出。
- **ko 运行时实拍**【实测】：toast「잘 들리지 않았습니다 음성이 감지되지 않았습니다 — 다시 시도하세요」；chip「"supplier"을(를) 사전에 추가해 자동 교정할까요?」（含韩语助词处理）正确。
- **zh-TW 运行时实拍**【实测】：toast「沒聽清 這次沒識別到內容，再說一次試試」；历史页（歷史記錄/複製 糾錯 刪除/查看辨識原文）；chip「把「clientr」加入詞典，之後自動糾正？」全繁体正确。
- 所有 chip 均点取消，词典未污染。

## ③ 核心回归（全过）

- RightCtrl 中文：「我明天去公园散步」准确落字【实测】。
- Alt+Q 免按：\"The review and the report are done today.\" 准确落字【实测】。

## 环境限制（如实挂账）

- 手机端为 ws 协议级模拟（同 116 轮方法），非真实手机浏览器/App；真手机麦、公网中转模式仍挂账。
- 五语实拍覆盖新增 toast/chip/历史页，未逐页全量截屏（357 键完整性已由脚本兜底）。

## 测毕清场

- SpeakType/notepad/node 进程退出；43117/18099 无监听；无 .part
- speaktype.json、history.json 由 round122-*.bak 整体还原（remote mic 关、autoLearn 关、词典清空、uiLanguage 还原、模型回 parakeet）
- 测试 pcm/wav/脚本产物留 review 工作区，用户数据无残留
- 防火墙三 profiles OFF；repo 回 main、工作区干净
