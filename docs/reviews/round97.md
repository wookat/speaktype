# 第 97 轮体验官审查报告 — #174 回归 + 多人设规则边界专项 + 悬浮条字幕抽样 + 核心回归

- 基线：main @ `34b5010`（含 #174/#175），`npm run pack:dir` 退出码 0，打包版实测
- 环境：Windows Server 2022；防火墙三 profile 全程 OFF；测毕清场（见文末）
- 口径：【实测】= 打包版运行实证；【源码】= 代码核对；【未验证/推测】= 如实标注

## 结论

| 级别 | 数量 |
|---|---|
| P0 | 0 |
| P1 | 0 |
| P2 | 0 |
| P3 | 0 |

零新立案。#174 回归通过；规则边界与悬浮条抽样全过；另回补第 96 轮一个未验证项（见 ①）。

## ① #174 回归 + 第 96 轮未验证项回补【实测】

- 托盘「Set up speech recognition」→ 直接打开主窗并落 设置→Speech 页签（Audio/Provider/Status Ready 一屏可见，实拍），不再弹豆包网页。第 96 轮 P3-① 修复生效。
- **回补：升级横幅缓存生成路径**——第 96 轮因本机 api.github.com 匿名 403 未能实测「成功才写缓存」；本轮限流解除后启动实测：日志 `latest release prefetched: v0.15.0` + `latest-release.json` 落盘 `{tag:"v0.15.0",at:…}`（实查）。#172 缓存写/读两条路径至此均已实测闭环。

## ② 多人设规则边界专项【实测+源码】

规则语义【源码 activeapp.ts:186-198】：match 对「进程名 + 窗口标题」合并串做不区分大小写的子串匹配，规则按列表顺序先命中先用，全不命中回落当前手动人设。

| 验点 | 做法 | 结果 |
|---|---|---|
| 大小写不敏感 | 规则写 `NOTEPAD.EXE`（全大写） | 命中 notepad.exe，条目 persona=To my boss ✓ |
| 重叠规则先后 | 同窗口同时命中「NOTEPAD.EXE→boss」「记事本测试→partner」两条 | 谁在列表前谁生效：boss 在前记 boss，交换顺序后记 partner（两次实测）✓ |
| 中文窗口标题 | 打开 `记事本测试.txt`，规则 match=记事本测试（纯标题串，非进程名） | 命中，persona=To my partner ✓（UTF-16 标题读取正确【源码 GetWindowTextW】） |
| 删除人设清洗 | UI 新建人设 TestX + 规则 chrome→TestX，再删 TestX | 规则随人设自动移除（settings 实查，仅剩 partner/boss 两条）✓【#107 清洗回归】 |

观察（不立案）：规则优先级=添加顺序，UI 无排序控件也无说明文案；实践中规则数少、可删重加绕过，属低频边缘。另外规则页「需配置 AI 润色模型才生效」黄条与 persona 仅记名不改文的实际行为一致（第 84 轮结论保持）。

## ③ 悬浮条字幕抽样【实测】

- **人设徽标**：记事本前台口述时悬浮条左侧显示命中人设徽标「To my partner」（实拍），与规则先命中先用一致。
- **长句滚动**：3 行档位下长句字幕在气泡内滚动、最新词保持底部可见（实拍）；不遮挡不溢出。
- **高度档位即时生效**：设置 Caption height 3 lines→6 lines 后**无需重启**，下一次口述气泡即为 6 行高度（实拍对比）。档位为 1/3/6 行三档。
- 识别中「Transcribing…」状态胶囊正常。

## ④ 核心回归【实测】

- RightCtrl 中文「今天下午3点开会，预算是5200元」（sensevoice+ITN，本轮两次落 Notepad）+ Alt+Q 免按「我们明天去公园散步」准确落字。

## 清场记录

- 临时人设 TestX 与 chrome 规则已随删除清洗；记事本测试.txt 已删；配置/历史从备份还原；latest-release.json/transcribe-last.json 已删。
- SpeakType 进程 0；无 .part；43117 无监听；防火墙三 profile 保持 OFF。

## 下轮候选

1. 长期未审面继续：词典页专项（300 上限边缘、批量删除、导入导出如有）/ 官网下载资产完整性复查。
2. 规则优先级说明文案（一句 helper text，若采纳属文案级）。
3. 云端成功路径补测（等 key）/ 真手机麦通道（挂账）。
