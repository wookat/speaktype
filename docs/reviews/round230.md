# 第 230 轮严格体验官报告（Round 230）

- 日期：2026-08-20
- 被测版本：main @ e8102de（含 PR #321），`npm run build` + `npx electron-builder --win nsis` 打包，per-user 安装 SpeakType-Setup-0.15.1.exe 实测
- 环境：Windows Server 2022，fake-mic WAV（time330.wav）+ 本地 mock ASR + CDP(9333)，无真实麦克风/真机
- 上轮报告：round228.md（PR #320）；上轮三 P3（2281/2282/2283）由 PR #321 修复合并

## 1. 回归 PR #321 + RightCtrl 【实测确认】

| 项 | 结果 |
|---|---|
| zh-TW 增強標點 hint（P3-2283） | 通过。UI 语言切 zh-TW，設定→語音識別 显示新 hint 全文（含「繁體中文與韓文輸入會改用內建規則斷句（模型不適用）」回退句）。截图见 PR 评论 r230_zhTW_hint.png |
| 导入 captionLines 值域校验（P3-2282） | 通过。导入 `{captionLines:2, holdDelayMs:450}`：captionLines=2 被拒（存储仍为原值），holdDelayMs=450 生效，横幅「已忽略 1 個欄位」计数正确。截图 r230_import_banner.png |
| RightCtrl 核心落字 | 通过。Notepad 落字 + history 新增一致 |
| 附带：简体标点/ITN 样例 | 通过（punct-ct 热态逗号 + 三点半→3:30 等） |

P3-2281（description 乱码）未复装 all-users 验注册表，仅确认 package.json description 已为英文【未测试】（NSIS 注册表级验证上轮测试代理已做）。

## 2. 专项：punct-ct 热态 + 繁体门混合输入 30 分钟级长会话内存 【实测确认】

方法：punct-ct 已下载热态、enhancedPunct 开启，繁/简/英三句轮流连续听写 45 次（RightCtrl 按住 4s + 25s 间隔），13:27–13:50 共 23.4 分钟，每次听写后采样 `Get-Process SpeakType` WorkingSet（soak230.ps1 → soak230.csv，46 个采样点）。

结果：
- 进程数全程恒定 9，无崩溃/无重启。
- 总 WorkingSet：基线 293.6MB → 首句峰值 679.5MB（punct-ct 模型加载）→ 13:36 起稳定在 327–346MB，稳定窗（iter18→final，14 分钟）总量 327.7→327.9MB，**斜率 ≈ 0.01MB/min**，远低于会话中基线 ≤0.15MB/min。
- 主进程（承载 punct worker）：峰值 399.6MB 后 OS 逐步回收 working set 至 ~58MB，稳定窗内 57.8→59.9MB 波动（中途一次 69.7MB 后自行回落），无单调上涨。
- 其余进程（renderers/GPU/utility 合并观测）最大单进程 41–46MB 全程平稳。
- 会话末三条 history 确认三语后处理仍正常：简体模型逗号、繁体走规则断句（繁体门无词内逗号）、英文句号——**繁体门路径无新泄漏**。

局限：脚本按「无 --type= 参数」区分主进程，panel 与字幕 renderer 未逐一分离（合并在 maxOther 观测且平稳）【实测确认合并值/未分离单值】；空闲斜率本轮未单测（第 217 轮已验 10 分钟 idle 释放）；soak 期间 itn 处于关闭状态（不影响内存路径结论）。

## 3. 自选专项：词典繁体热词纠错实效

热词集：`資料庫優化 / 陳慧琳 / 張京 / 張靜`（CDP updateSettings 注入），mock ASR 注入文本，打包运行时端到端：

| # | 输入（mock ASR 输出） | 实际落字 | 判定 |
|---|---|---|---|
| T1 | 請把資料庫**尤化**的報告先準備好 | 資料庫**優化** | 繁体同音纠错生效【实测确认】 |
| T2 | **陈惠临**明天来开会（简体） | **陳慧琳**明天来开会 | 跨简繁同音命中（pinyin 读音层匹配）【实测确认】 |
| T3 | **張靜**明天會把資料先準備好 | 張靜（不变） | 同音词典词互噬保护在繁体下有效【实测确认】 |
| T4 | 三點半開會預算兩千五百塊…資料庫尤化… | 3:30開會預算2500塊，…資料庫優化… | 繁体 ITN(#317)+繁体门规则断句(#319)+热词三叠加无互搏【实测确认】 |
| T5 | **晨會臨**時取消明天再說 | **陳慧琳**時取消… | 跨词边界同音误伤 → 立案 P3-2301【实测确认】 |

T5 在简体（晨会临时取消→陳慧琳时取消）同样复现，属中文同音替换通用类问题，非繁体门引入。

补充发现：测试中发现 itn 设置处于 false（疑为本轮早前导入测试残留），开启后 T4 全部转换正常；ITN 关闭时热词/标点不受影响。

## 4. 立案

| 编号 | 级别 | 描述 | 证据 |
|---|---|---|---|
| P3-2301 | P3 | 中文热词同音替换跨词边界误伤：三字人名热词把句首同音片段吸走（晨會臨時取消→陳慧琳時取消，简繁同现）。hotwords.ts 逐窗口同音匹配无词边界/上下文校验。建议：命中片段两端字符与热词组成词语时（可用简单双字词表或要求边界非 CJK 连续词中段）跳过，或人名类热词要求前后邻接非同词素 | 第 3 节 T5【实测确认】 |

本轮无 P0/P1/P2。

## 5. 下轮 Top3 建议

1. P3-2301 修复后回归（含 T1–T5 全组 + 第 217 轮简体 5 句无误伤集）。
2. 历史页繁体条目搜索/筛选（本轮自选未覆盖的另一候选）。
3. 真机麦克风/真手机 remoteMic 端到端（长期缺口）。

## 6. 清场

测试 history 清空、热词清空、enhancedPunct 关闭并删 punct-ct 模型、itn/holdDelayMs/UI 语言恢复默认、mock ASR 与 Notepad 退出、SpeakType 进程退出、临时 json/csv/截图留存于 tts 工作目录（不入库）、per-user 0.15.1 安装保留为开发基线、仓库工作区干净。

## 7. 未测试范围

真实麦克风与真机、all-users 注册表复验（P3-2281 运行时级）、空闲态 30 分钟斜率、panel/字幕 renderer 分进程斜率、doubao/chatgpt 真实账号链路。
